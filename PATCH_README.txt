Miner v0.5.3 interaction patch

Copy EVERYTHING in this folder into the root of your local GitHub repository (ost) and allow overwrite.
Then GitHub Desktop: Commit to main -> Push origin.

Change: miner right-click now uses the normal entity interaction packet. Client returns PASS; server validates miner and sends market snapshot.
